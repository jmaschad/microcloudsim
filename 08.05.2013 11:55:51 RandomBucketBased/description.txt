Configuration description:
duration = 6.307E7

selector = RandomBucketBased
replica count = 3

cloud count = 30
cloud bandwidth dist = NormalDist(125.0,20.0)

user count = 500
bucket count = 100
object size dist = ExponentialDist(30.0)
mean get inteval dist = NormalDist(2.0,0.5)
object for get selection = ZipfSelection()
